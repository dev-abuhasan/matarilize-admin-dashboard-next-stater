'use client';

import { useEffect } from "react";

import { getCompanies } from "@/contexts/api";

const Test = () => {

    // useEffect(() => {
    //     const getData = async () => {
    //         const res = await getCompanies();

    //         console.log(res)
    //     }

    //     getData();
    // }, [])

    return (
        <div>
            <h1>Tes</h1>
        </div>
    );
};

export default Test;
